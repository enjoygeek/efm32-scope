/*
 * catch_unpack.cpp
 *
 *  Created on: Apr 29, 2017
 *      Author: bulashevich
 */

#define CATCH_CONFIG_MAIN
#include "catch.hpp"

//#include <vector>
//#include "../protocol.h"

//TEST_CASE( "Unpacked matches source", "[string]")
//{
//	std::string str = "Hello protocol";
//	size_t len = str.length();
//	std::vector<uint8_t> buf(len*2);
//	std::vector<char> unpacked(len*2);
//	pack_string(buf.data(), buf.size(), str.data());
//	unpack_string(buf.data(), unpacked.data());
//
//	REQUIRE(str == std::string(unpacked.data()));
//}

